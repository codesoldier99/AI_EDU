# 开发方案 DEVELOPMENT_PLAN

苏格拉底式 AI 教学系统 · 详细技术开发计划

> 配套文件：`CLAUDE.md`（架构铁律与开发规范，每次开工必读）
> 实现进度见文末【实施记录】；架构决策的理由见 `docs/architecture.md`。

---

## 〇、本计划的两个来源

**来源一：全球调研的硬约束**（详见《全球AI教育系统调研分析报告》）

| 调研发现 | 设计对策 |
|---|---|
| Khanmigo 仅 15% 学生经常使用，最大难题在行为不在算法 | 教师侧先行；**主动使用率提前到 Phase 1 验收指标** |
| 无限制 AI 辅助使独立成绩下降约 17%，引导式显著更优 | 追问不给答案写成硬约束；**禁止把正确率作为优化目标** |
| 生成式模型不适合精确计算（可汗被迫外挂计算器） | 数值/代码正确性一律走确定性工具 |
| Alpha School 因拒绝独立验证被击穿 | 事件流只追加、状态可重算，按"可被第三方核查"设计 |
| Jill Watson 绑定课程材料后准确率是通用助手 2.5 倍 | 所有生成必须经 RAG 约束在教材与项目材料内 |
| 仅极少数系统有 RCT 级证据；中文场景空白 | Phase 4 内置对照实验数据采集 |

**来源二：范式调整**

传统智能教学系统按依赖顺序排课（先学半年螺丝刀再拆引擎）。本系统改为**拉取式**：
知识由项目需求触发，学完立刻用回项目。实验班本就是项目驱动、五到十个真实项目，
天然适配这一范式。

---

## 一、总体架构

```
┌─────────────────────────────────────────────┐
│ L3 生成层（可替换）                          │
│ 诊断 / 追问 / 任务 / 审查 / 画像 / 副驾驶     │
└──────────────┬──────────────────────────────┘
               │ 只读状态，写事件
┌──────────────▼──────────────────────────────┐
│ L2 学生状态层（可积累·核心资产）             │
│ BKT 掌握度 · 能力画像 · 事件流 · 错误模式库   │
│ ┌─ engagement（派生态，只读事件流，不回写）  │
└──────────────┬──────────────────────────────┘
               │ 只读图谱
┌──────────────▼──────────────────────────────┐
│ L1 知识与能力图谱层（可积累）                 │
│ 知识点 · 依赖 DAG · 能力模块 · 任务-知识点映射 │
└─────────────────────────────────────────────┘
```

**核心闭环（拉取式）：**

```
项目任务 → 缺口计算 → 定向学习 → 引导追问 → 回项目实做
    ↑                                              ↓
    └──────── 画像/诊断 ←── 审查（工程问题翻回知识点）
```

审查环节是把实践信号翻回知识判断的**唯一通道**，缺了它项目与课程仍是两张皮。

---

## 二、Phase 0：地基（4 周）

**目标：** 数据库跑通、首门课图谱入库、**任务-知识点映射建立**、LLM 封装可用。

### 0.1 数据库 Schema

关键约束：

```sql
-- 依赖边无环
ALTER TABLE prerequisite ADD CONSTRAINT no_self_loop
  CHECK (from_kp_id <> to_kp_id);

-- 事件流只追加
REVOKE UPDATE, DELETE ON learning_event FROM app_user;

-- 掌握度可追溯到事件
ALTER TABLE mastery_state ADD COLUMN last_event_id BIGINT
  REFERENCES learning_event(id);
```

**验收：** `make replay STUDENT=<id>` 从事件流完整重算掌握度与 engagement，与当前值一致。

### 0.2 知识图谱导入

试点课程建议 **机器学习原理与应用**（结构清晰、题目可自动判定）。

标注规范（写入 `docs/kp-annotation.md`）：
- 粒度准则：**能独立命题、能独立判定会不会**
- 单课程目标 300–500 个知识点
- 依赖边只标强依赖（"学 B 前必须先会 A"），弱相关用 `related`
- 每个知识点挂 1–3 个能力模块（M1–M8）

导入流程：大模型预抽取 → 教师审核 → 依赖边半自动生成 → 环检测 → 入库。

**学生参与点：** 知识点标注适合入门任务，接口清晰、可独立验收。

### 0.3 任务-知识点映射（拉取式的前提）★

这是本次范式调整新增的关键工作，**没有它拉取式无从谈起**。

```
对每个项目：
  拆解为任务树（ProjectTask）
  为每个叶子任务标注所需知识点（TaskKPLink）
  necessity ∈ {required, helpful}
```

首批只做 1–2 个项目（建议 AGV 与一个视觉类项目），验证映射粒度是否可用。

**注意：** 任务所需知识点由**导师标注**，不由大模型自动生成——这是学分认定与能力
追溯的依据，必须可靠。大模型可给候选，教师确认。

### 0.4 LLM 封装层

```python
class LLMClient(Protocol):
    async def complete(self, messages, *, model_hint, max_tokens) -> LLMResponse
    async def embed(self, texts) -> list[Vector]
# 必须记录 model_name, model_version, prompt_hash, token_usage
# embedding 记录模型版本，换模型可批量重算
```

**验收：** 切换底座模型只改配置，不改业务代码。

---

## 三、Phase 1：教师减负 + 诊断 + 参与度基线（6 周）

**目标：** 教师第一次能在课后当天看到全班卡在哪里；同时建立参与度观测。

### 1.1 基础文档生成

培养方案 → 教学大纲、授课计划、教案、试卷、实验任务书/指导书。

**关键：不是一次性输出，必须回流：**

| 产出 | 回流去向 |
|---|---|
| 教学大纲 | 写回 L1，补充知识点图谱 |
| 试卷题目 | 每题挂知识点标签，作答回流 L2 |
| 实验任务书 | 挂能力模块与任务节点，过程数据回流 L2 |

试卷生成须支持**按薄弱点命题**——与 1.2 的接口。

### 1.2 班级诊断报告（本阶段核心）

**根因回溯算法：**

```
对每个掌握度 < 阈值的知识点 K：
  沿 prerequisite 反向 BFS
  若前置点 P 掌握度也低 → 递归
  返回最深的低掌握度节点作为根因
  例：反向传播出错 → 回溯到链式法则
```

**报告字段**（须教师确认后定稿）：
- 全班知识点掌握热力图
- **Top 3 待攻克点 + 各自根因**
- 典型错误归因（来自错误模式库）
- 下节课建议重点（教师可一键否决）
- 需个别介入的学生名单
- **本周主动使用率与降级热点**（新增，见 1.4）

**BKT 基线：** 标准四参数，课程级默认值起步。单知识点作答满 200 次后考虑参数拟合；
事件量达万级再评估是否切 DKT。

### 1.3 错误模式库 v1

**系统最有价值的资产，从第一天开始积累。**

```
学生答错 → ErrorInstance（原始作答）
        → 大模型归因，聚类到 ErrorPattern
        → 教师确认/修正（teacher_verified）
        → 沉淀为该知识点的典型错误
```

Phase 1 只做采集与人工确认，聚类可粗糙。**关键是从第一届学生就存下来。**

### 1.4 参与度基线（从 Phase 3 前移）★

Khanmigo 的教训要求我们**在第一阶段就开始观测行为**，而不是等系统建完才发现没人用。

```python
# packages/engagement —— 只读 LearningEvent，产出派生态
StreakState:  连续活跃天数、最长记录、最后活跃日
Achievement:  breakthrough(攻克根因点) / milestone(项目里程碑)
              / comeback(中断后回归) / first_run(真机首次跑通)
```

**Phase 1 只做采集与展示，不做激励机制**（激励放 Phase 3，与学生端一起上）。

**新增验收指标：教师周主动打开诊断报告 ≥ 3 次。**
若教师自己都不看，学生端更无从谈起。

---

## 四、Phase 2：追问引擎 + 代码审查（8 周）

### 2.1 追问策略引擎

**不交给大模型自由发挥。** 引擎依据 L2 状态决定三件事：

```python
class AskingStrategy:
    def decide(self, student_state, target_kp) -> AskingPlan:
        # 1. 起点：从已掌握的最近前置点切入
        entry = find_nearest_mastered_prerequisite(student_state, target_kp)
        # 2. 深度：掌握度越低，拆解越细
        depth = compute_depth(student_state.mastery[target_kp])
        # 3. 类型：概念澄清/反例质疑/类比迁移/条件变更
        style = select_style(target_kp.type, student_state.history)
        return AskingPlan(entry, depth, style)
```

**RAG 约束：** 所有追问必须基于课程知识库检索结果生成，禁止脱离教材自由发挥。
（依据：Jill Watson 绑定课程材料后准确率 78.7% vs 通用助手 30.7%）

**降级机制（必须实现且必须留痕）：**

```
尝试 N 次未进展 或 检测到挫败信号
  → L1 关键提示
  → L2 解题框架
  → L3 完整解析 + 标记该知识点需教师介入
每次降级写 LearningEvent(event_type='escalation')
诊断报告显示降级热点
```

> ⚠️ 降级是为了**防止挫败感摧毁动机**，不是为了让学生轻松。
> 阈值 N 与挫败判定标准由教师团队给经验值，代码做成可配置。

### 2.2 代码与文档审查（两级流水线）

```
第一级 静态分析（确定性、成本低）
  ruff/pylint · 复杂度 · 测试覆盖 · 依赖分析 · 安全扫描
        ↓ 结果作为上下文
第二级 大模型评审
  逻辑缺陷 · 设计合理性 · 文档完整性
  检索项目知识库历史方案做对照
```

**输出必须结构化，且必须挂知识点：**

```python
class ReviewFinding(BaseModel):
    file: str
    line: int
    category: Literal['style','logic','design','doc','security']
    severity: Literal['info','warn','error']
    message: str
    suggestion: str
    related_kp_ids: list[int]   # ← 回写 L2 的唯一通道
```

`related_kp_ids` 是把工程问题翻回知识判断的唯一接口。
**若某类缺陷无法可靠映射到知识点，则不回写，宁缺毋滥。**

**教师协同：** 审查结果为建议，导师可采纳/修改/否决；否决记录用于优化规则。

---

## 五、Phase 3：拉取式调度 + 项目接入 + 学生端（8 周）

### 3.1 拉取式任务调度 ★（本次范式调整的核心）

**旧逻辑（已废弃）：**
```python
next_task = topological_next(task_graph, completed)   # 按拓扑序推
```

**新逻辑：**
```python
def next_learning_target(student, current_task) -> list[KnowledgePoint]:
    required = get_required_kps(current_task)             # TaskKPLink
    gap = [kp for kp in required
           if mastery(student, kp) < THRESHOLD]
    # 缺口内部再按依赖拓扑排序（拓扑序只在此处使用）
    gap = topological_sort(gap)
    # 按挡路度排序：阻塞后续任务越多，优先级越高
    return rank_by_blocking_severity(gap)[:2]             # 一次只给 1–2 个
```

**挡路度定义：** 某知识点被多少个待办任务依赖 + 它阻塞多少后续知识点。

**产品层面的连带要求：**
- 去掉"课程进度百分比"，只显示知识点掌握度与项目进度
- 入学诊断后**允许跳跃**：已掌握的直接跳过，不陪跑
- 出口仍是统一的 30/70 考核——过程完全个体化，出口有门槛

**调试工具：** `make gap STUDENT=<id> TASK=<id>` 打印缺口与挡路度排序。

### 3.2 项目适配器

```python
class ProjectAdapter(Protocol):
    project_type: str
    def collect(self, since: datetime) -> list[ProjectSignal]:
        """将本项目原始数据归一化为五类标准信号"""
```

信号来源举例：

| 项目类型 | runtime 信号 |
|---|---|
| 机器人与移动平台 | 任务跑通率、传感器日志、定位精度 |
| 机器视觉 | 数据集迭代记录、模型精度曲线 |
| 大模型应用 | 提示词版本、评测集得分、推理时延 |
| 数据平台 | 管道稳定性、查询性能 |
| 嵌入式与边缘 | 资源占用、功耗、实时性 |

**五类信号 → M1–M8 权重矩阵**由教师团队标定，写成配置文件而非硬编码。

### 3.3 激励系统（只奖励坚持）★

```python
# 允许的激励事件
breakthrough  攻克一个根因知识点        权重最高
comeback      中断 ≥3 天后重新活跃      权重次高（防流失关键）
milestone     项目里程碑达成
first_run     真机/系统首次跑通
streak        连续活跃天数

# 明令禁止的激励对象
❌ 正确率  ❌ 做题数量  ❌ 停留时长  ❌ 排名（避免挫伤后进者）
```

**设计要点：**
- `comeback` 必须给显著正反馈——流失往往发生在中断后的第一天
- 破关提示要与**根因**绑定："你攻克了链式法则，这是之前 3 个卡点的共同原因"
- 不做全班排行榜，只做个人成长曲线

### 3.4 学生端：追问入口 + 学习副驾驶

副驾驶与通用工具的三点差异（必须实现，否则无意义）：
1. 接项目知识库做 RAG，回答绑定本院真实项目
2. 读个人能力档案，输出针对性建议
3. 主动同步提交记录，无需学生手动上传

**周期简报：** 学生版重"下一步建议 + 本周破关"，教师版重"异常与需介入事项"。

> ⚠️ **本阶段最大风险仍是使用率。**
> 对策：追问入口嵌入作业与项目流程内（不做独立 App）；简报主动推送；
> 每周统计活跃度作为迭代依据。**验收标准：学生周活跃率 > 50%。**

---

## 六、Phase 4：画像、映射与效果验证（6 周）

### 4.1 学习成效画像

能力雷达图 · 成长曲线 · 知识覆盖图（图谱上标掌握深浅）· 项目贡献画像。

用途：导师精准指导、项目分组、动态分流与替补管理、学分认定佐证。

### 4.2 项目-课程学分映射

教师提交项目描述 → 语义匹配 L1 能力图谱 → 建议课程与学分上限 → 学术委员会审核。

**必须叠加规则约束**（单项目学分上限、必修课不可完全替代），映射可追溯到图谱节点。

### 4.3 效果验证设计（战略价值最高）

调研结论：全球仅极少数系统有 RCT 级证据，中文工科场景近乎空白。
**这是最容易做出国际级贡献的位置。**

从 Phase 1 起按可发表标准采集：
- 实验班 vs 对照组（同专业普通班）
- **延迟后测**（间隔数周）作主指标，**不用当场正确率**
- 多指标交叉：延迟后测、迁移任务、项目产出质量
- 研究方案预注册

> ⚠️ 若系统以"提高当场答对率"为目标，会训练出让学生越学越弱的系统。
> **评测指标的选择本身就是架构决策。**

---

## 七、学生参与的模块划分

系统研发本身作为实验班实战项目。**接口清晰、可独立验收的交给学生；决定骨架的由导师把关。**

| 模块 | 归属 | 难度 | 覆盖能力 |
|---|---|---|---|
| 知识点标注与图谱构建 | 学生 | ★ | 知识梳理、数据工程 |
| 项目适配器开发 | 学生分组并行 | ★★ | 接口设计、数据处理 |
| 画像看板前后端 | 学生 | ★★ | Web 全栈 |
| 静态分析工具链集成 | 学生 | ★★ | 工程规范 |
| 激励系统与参与度统计 | 学生 | ★★ | 事件流处理 |
| RAG 检索优化与召回评测 | 学生（进阶） | ★★★ | 检索、评估方法 |
| 追问策略引擎 | 导师带核心学生 | ★★★★ | 算法设计 |
| **状态模型与层间接口** | **导师** | ★★★★★ | 做错返工代价极高 |
| **任务-知识点映射标注** | **导师** | ★★★ | 学分认定依据，须可靠 |

---

## 八、里程碑与验收

| Phase | 周期 | 关键验收标准 |
|---|---|---|
| 0 地基 | 4 周 | 事件流重算一致；切模型不改业务代码；首个项目任务-知识点映射完成 |
| 1 诊断 | 6 周 | 课后 30 分钟出报告；教师认可至少一条"我没注意到"的结论；**教师周打开 ≥3 次** |
| 2 追问审查 | 8 周 | 追问不直接给答案；代码缺陷可靠挂知识点并回写 L2 |
| 3 拉取+项目 | 8 周 | `make gap` 输出合理；第 5 与第 10 个适配器工时相当；**学生周活跃 >50%** |
| 4 画像验证 | 6 周 | 产出可提交的对照实验数据集 |

**总周期约 32 周**，与实验班两年周期并行推进。

---

## 九、待教师团队拍板的事项

以下参数决定系统形态，**代码一律做成配置，但初值需教学经验给定**：

1. 知识点拆解粒度——一门课 300 还是 500 个？
2. 试点课程与首批接入的 1–2 个项目？
3. 掌握度阈值——低于多少判定"未掌握"、进入缺口？
4. 追问降级阈值——尝试几次后降级？各级给多少提示？
5. 五类项目信号 → M1–M8 的权重矩阵
6. 诊断报告字段——教师课后第一时间最想看到哪几件事？
7. 代码缺陷→知识点的映射规则——哪些缺陷可以可靠回写？
8. **任务-知识点映射的标注粒度**——一个叶子任务挂几个知识点合适？

---

## 十、风险登记

| 风险 | 影响 | 对策 |
|---|---|---|
| 学生端使用率低（Khanmigo 前车之鉴） | 高 | 教师侧先行；参与度观测前移到 Phase 1；嵌入既有流程；只奖励坚持 |
| 任务-知识点映射工作量大 | 高 | 首批只做 1–2 个项目验证粒度；大模型给候选、教师确认 |
| 拉取式导致知识零散、缺乏体系 | 中 | 保留知识覆盖图；期末按图谱做体系性补漏；出口统考不变 |
| 知识图谱标注超预期 | 中 | 大模型预抽取＋教师审核；学生参与；先做一门跑通 |
| 冷启动期诊断不准 | 中 | 显式 confidence；前 3 周标注"数据不足" |
| 教师抵触 | 高 | 建议非指令、一键否决、不生成教师评价数据 |
| 大模型换代返工 | 中 | 封装隔离；可积累数据不依赖模型格式 |
| 学生代码难集成 | 中 | 接口契约先行；每模块明确验收标准 |

---

## 附：本次修订说明（v2）

相对 v1 的三处实质改动：

1. **调度逻辑由推送改为拉取**（Phase 3.1）——知识图谱角色从"教学顺序表"变为
   "按需检索索引"；新增 `TaskKPLink` 数据结构与 Phase 0.3 映射标注工作。
2. **新增激励系统**（Phase 3.3）——明确只奖励坚持类行为，禁止奖励正确率/题量/时长。
3. **参与度观测从 Phase 3 前移至 Phase 1**——Khanmigo 的教训是行为问题比算法问题
   更致命，必须早观测。

同时在 `CLAUDE.md` 新增架构铁律第 5 条（禁止以当场正确率或降低难度为优化目标），
并把"合意困难"与"无意义痛苦"的区分写入设计范式。


---

## 实施记录（截至 2026-07-31）

| Phase | 状态 | 落地位置 | 验收方式 |
|---|---|---|---|
| 0 地基 | ✅ | `migrations/`、`packages/{graph,state,llm}`、`data/seed/` | `make replay` 全班一致；假模型客户端证明切模型不改业务代码；首批 2 个项目 115 条任务-知识点映射 |
| 1 诊断 | ✅ | `packages/agents/diagnosis.py`、`packages/errors/`、`packages/engagement/` | 班级报告含 Top3 + 根因 + 降级热点 + 主动使用率；`report_open_log` 统计教师打开次数 |
| 2 追问审查 | ✅ | `packages/agents/{asking,review}.py` | 未降级不给答案；降级写事件；只有 reliable 映射经教师采纳才回写 L2 |
| 3 拉取+项目 | ✅ | `packages/agents/task.py`、`packages/adapters/`、`apps/web/` | `make gap` 输出合理；新增适配器核心代码 md5 不变；学生端与副驾驶可用 |
| 4 画像验证 | 部分 | `packages/agents/profile.py` | 画像已上线；**对照实验数据采集尚未开始** |

### 与原计划的差异及理由

1. **技术栈先做零依赖版**（标准库 HTTP + SQLite 替代 FastAPI + PostgreSQL）。
   目的是让演示环境与生产环境跑同一套逻辑，且评审现场无需任何部署前置条件。
   所有可替换件都在接口后面，迁移时业务代码不动。
2. **`make seed-tasks` 合并进 `make seed`**：任务-知识点映射与课程图谱同属种子数据，
   分两条命令没有实际收益。
3. **新增 `make check`**：把架构铁律做成可执行检查，而不是只写在文档里。
4. **重算的规范顺序定为事件记录顺序（自增 id）而非 occurred_at**，
   理由见 `docs/architecture.md` §3——否则重算不可判定。

### 下一步

- Phase 4 的对照实验数据采集设计（实验班 vs 同专业普通班，延迟后测为主指标，研究方案预注册）。
- `docs/decisions.md` 中 13 项参数的教师团队拍板。
- 首批知识点标注由 183 个细化到 300–500 个。
